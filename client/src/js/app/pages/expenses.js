import React from "react";
import MoneyForm from "../money-form/";

const expenses = () => {
    return (
        <div className="section">
            <MoneyForm />
        </div>
    );
};

export default expenses;
